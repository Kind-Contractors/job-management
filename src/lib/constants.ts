// Shared literal strings used by both the mock and the real repository, kept
// here so the production repository never needs to import from src/mock/.

export const DEFAULT_ACCESS_NOTE = 'Access details not yet recorded for this building.';
